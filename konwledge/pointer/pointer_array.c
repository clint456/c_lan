//将若干个字符串按照字母顺序（由小到大）输出
#include <stdio.h>
#include <string.h>

int main()
{
    void sort(char *name[],int n);
    void print(char *name[],int n);
    char *name[] = {"Follo me","BASIC","Great Wall","FORTRAN","Computer design"};

    int n = 5;
    

    return 0;
}

void sort(char *name[],int n)
{
    char *temp;
    int i,j,k;
    for (i=0;i<n-1;i++)
    {
        k=i;
        for(j=i+1;j<n;j++) 
    }
}

void print(char *name[],int n)
{

}